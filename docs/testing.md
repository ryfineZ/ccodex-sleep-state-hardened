# 测试记录

> **先区分版本。** 最上方是 v0.4.0 发布前隔离验收；其后的记录属于此前轮次。旧轮次成功不能覆盖新场景失败。此前 Team、Sol/Terra、CCS 恢复、损坏配置修复和远程压缩的详细状态见[问题与验收清单](issues-and-verification.md)。普通聊天成功不等于远程压缩成功。

## v0.4.0 发布前隔离验收 · 2026-09-19

本次重新运行 `go vet ./...`、`go test -race -count=1 ./...` 和三个前端脚本语法检查，全部通过。随后对 `ddc9584` 对应的 macOS Apple Silicon 原生预览二进制做跨进程验收；发布准备仅改文档，应用源码未变。

**48 项检查通过**：基础链路 33 项、固定策略 7 项、主备策略 8 项。使用全新的服务数据目录、独立 HOME / CODEX_HOME、动态回环端口、本地模拟上游/订阅/代理与合成凭据。没有接管日常配置、没有读取真实 `apikey.txt`，也没有再次请求此前 403 的真实中转。

- `init` 新用户默认固定策略正确；三次服务启停，配置接管及字节级恢复正确。
- 一键环境检测前后文件内容、mtime 和模拟上游计数不变；只读、不额外外联。
- 中转三模型列表筛选、模拟 Responses 成功、官方专用头不泄漏；17 MiB 合法 gzip 请求通过。
- 多订阅去重、一次性随机池、用尽时不联网、已用/停用记录重启持久化、批量回收均通过。
- 模拟 403 只派发一次；之后跨模型停止，池回收不能解除拒绝，不自动重放。
- 固定模式首次采得合成 292 后，等待 32 秒跨过测试冷却与后台检查，探测总数仍为 1。模拟 312 正文被拦截为本地 503，不重放；下一请求才获取第二份 292 并成功。
- 主备模式等待真实后台周期与冷却（约 60 秒）取得第二份 292，不提前替换主用。312 正文拦截后，下一请求使用备用，探测总数仍为 2。
- 两个验收脚本均以 0 退出，自建服务和模拟进程全部停止。

以上是本机原生进程与回环合成证据，不是实际账号取得 292 的证明。Windows / macOS / Ubuntu 回归由提交和标签 CI 分别验收；四平台交叉构建不等于四类硬件都做了真人桌面联调。真实中转生成仍保留下面的 403 限制。发布内容见[发布说明](release-v0.4.0.md)。

## 0.4.0 分支预览：本轮证据单独记录

此节只描述 `codex/pool-upgrade`，不等于正式版本验收；下方 v0.3.x / 更早版本的历史成功不能替代本轮新增功能验证。

### 本地新增回归

新增测试使用临时目录、合成凭据和本地模拟服务，覆盖：

- 节点池记录持久化、损坏记录拒绝使用、并发预留同一随机节点最多一次。
- 固定可用 state 不受提前刷新窗口替换，到期仍失效，旧响应不能清除更新后的值。
- 正式响应形状不匹配时阻止正文、提升备用供下次使用，不自动重放原生成。
- 独立随机选择跳过采集节点及已用/失败节点；选定单节点重试不破坏现有主用值。
- 合法大于旧 16 MiB 边界的 gzip/zstd 请求可按新上限处理，继续约束解压和窗口。
- 缺失中转 `base_url` 的局部补全、保留其他 TOML 配置与拒绝不安全定位。
- 服务配置接口的鉴权、参数校验、备份及原有拒绝/限流保护。

本轮本地全量 `go test ./...`、`go vet ./...` 与 `go test -race -count=1 ./...` 已通过。额外回归覆盖：写盘失败仍保留 401/403/429、自动探测与正式请求原子争用、固定模式后台只采一份、旧固定路由清除冲突设置，以及停用节点在普通/压缩/模型列表请求中均不派发。浏览器已验证手动粘贴、TXT 载入、去重后两节点导入、清单全选停用与回收、高级预设保存、固定策略保存和一键只读检测；使用隔离目录与本地模拟订阅，没有对这些假节点发送模型请求。**本节不是对远端 CI 的预判**：提交对应的各平台构建结果以实际运行记录为准。[查看分支工作流](https://github.com/gylive/ccodex-sleep-state/actions)

### 真实中转：模型列表成功，生成未通过

使用维护者授权的专用本地测试配置，不公开 URL、密钥或原始响应。此次测试没有使用官方登录凭据，没有为中转采集官方 state。

| 操作 | 结果 | 结论 |
| --- | --- | --- |
| `GET /models` | HTTP 200，列出 `gpt-6-astra`、`gpt-5.6-sol`、`gpt-5.6-terra` | 本次模型列表可读，支持三模型筛选 |
| 经本地网关的唯一一次 Sol 短生成 | HTTP 403，立即停止 | 本次真实中转生成未通过；未继续试 Terra/Astra |

没有根据列表或 403 推断该站的账号池类型；没有把 332 当作 Team 身份证据，也没有声称 292、生成或远程压缩验收通过。后续需要服务商先处理权限/配置，再经明确授权另行验收，不能换节点继续撞。

## 本轮新增验收入口

- CLI 的 `setup` / `doctor` 已完成包级 `go test -race -count=1` 与 `go vet`；macOS 启动脚本通过语法、空格路径和参数传递测试。
- `gateway`、`settings`、`turnstate` 本轮 `go test -race` 和 `go vet` 已通过，包括个人/Team、三模型隔离、V1/V2 压缩、严格/兜底、手动重试与跨模型限流。服务、配置恢复及浏览器结果继续在问题清单更新，不借用下面的 v0.2 成功记录。
- 实际 Codex CLI `0.154.0` 通过 app-server 连接本地模拟上游，V1、默认 V2 和旧名称对照三组均通过；详见下节。
- 本轮原始 8 次真实尝试，追加 Sol 桥接阶段 1 次失败和 Astra 2 次短回复后，共保留 11 条账本：Sol 回复、V2 远程压缩及压缩后回复、Terra 回复成功；Astra 后续两次短回复成功，但仍未采到 292；V1 直连压缩返回上游 404，本地桥接模拟通过，真实桥接阶段首条 Sol 回复出口传输失败。Team 仍仅有合成回归，不据此发布“全部通过”。


## 本轮：真实 OAuth 请求验收（原 8 次 + 追加 3 次）

在独立服务数据目录、Codex Home 和工作目录中，用实际 Codex CLI `0.154.0` 及原有登录的只读副本联调。没有切换系统代理、刷新登录或使用重置卡。下面 11 次是同一个有限预算的累计账本，不是每遇到问题就重新开一轮。

此前 zstd 导致的本地 415 发生在上游之前，模型尝试 0 次，单独记录在后文。修复请求编码后，实际发向上游的是：

| 次数 | 模型与用途 | 真实结果 | 可以得出的结论 |
| --- | --- | --- | --- |
| 1 | Astra state 探测 | HTTP 200，返回 312 字符 / 11 块；没有完整 `response.completed` | 不接纳候选；严格模式返回本地 503，没有采到 292 |
| 2 | 关闭本服务注入后的 Astra 正式回复 | HTTP 200，但 SSE 内失败；客户端显示 `serverOverloaded` | 不是完整回复，也不是 HTTP 429；不能把容量错误当成代理断网或模型成功 |
| 3 | Sol 正式回复 | HTTP 200，SSE 完整结束，回复 OK | 该次官方 OAuth 普通转发成功 |
| 4 | Sol V2 远程压缩 | `/responses` + `compaction_trigger`，HTTP 200，压缩任务完成 | 该客户端与实际上游的 V2 压缩链路成功 |
| 5 | Sol 压缩后的下一条回复 | HTTP 200，SSE 完整结束，回复 OK | 不只压缩接口回 200，客户端压缩后还能继续对话 |
| 6 | Terra 正式回复 | HTTP 200，SSE 完整结束，回复 OK | 该次 Terra 官方 OAuth 普通转发成功 |
| 7 | Sol V1 验收前的普通回复 | HTTP 200，SSE 完整结束，回复 OK | 同次 V1 验收前普通链路可用 |
| 8 | Sol V1 远程压缩直连 | `/responses/compact` 返回上游 HTTP 404 | 该上游不接受本次直接 V1 请求；V1 真实验收失败，没有追加重放 |
| 9 | 追加桥接阶段 Sol 普通回复 | 出口传输失败，按规则停止 | 没有继续消耗后两次预算，桥接真实链路不宣称成功 |
| 10 | Astra 短回复 | HTTP 200，SSE 完整结束，回复 OK | 证明本次出口和 Astra 短回复链路可用；不等于采到 292 |
| 11 | Astra 第二次短回复 | HTTP 200，SSE 完整结束，回复 OK | 第二次短回复也成功；不等于 Team 或远程压缩验收 |

第 2–8 次关闭了本服务注入，以便区分模型/压缩链路和 state 筛选问题。这些成功**不是 Sol/Terra 的采集或注入成功证明**。返回的 312 不被当成 292 使用，也不拿来做模型质量结论。

累计到第 6 次，Sol V2 与 Terra 阶段通过；V1 直连失败后追加桥接首条请求失败；第 10、11 次 Astra 短回复成功。整轮仍未全部通过。不能只拿中途成功的记录宣告最终验收完成。

测试前后，原 Codex 配置、原登录文件、原代理配置均未改变；独立测试配置已恢复，临时凭据副本已删除。公开记录不包含节点地址、账号、认证值或完整 state。

**目前真实确认：**Sol 普通回复、V2 压缩及压缩后回复、Terra 普通回复、追加两次 Astra 短回复。**目前未确认：**本轮合格 292 采集/注入、真实 Team，以及真实 V1→V2 桥接。V1→V2 适配已完成本地验证；真实桥接阶段曾因出口传输失败停止，不能借 404 无限尝试。

## 本轮：真实客户端 + 模拟上游的压缩验收

这次运行的是实际 Codex CLI `0.154.0`，不是自己写一个 HTTP 请求冒充客户端。上游是本地测试服务，API key 认证与 state 都是合成 fixture，真实模型请求 **0 次**。**它不是实际 ChatGPT OAuth 链路测试。**

| 场景 | 观察到的行为 | 结果 |
| --- | --- | --- |
| 新托管配置 + V1 | provider 名称 `OpenAI`；客户端发 `/responses/compact`，收到压缩完成，压缩后继续回复 OK | 通过 |
| 新托管配置 + 默认 V2 | provider 名称 `OpenAI`；客户端发 `/responses`，input 包含 `compaction_trigger`，收到压缩完成，压缩后继续回复 OK | 通过 |
| 旧名称对照 | 刻意使用旧展示名称，客户端不发 V1/V2 远程压缩，而走普通 Responses 本地总结路径 | 差异复现，验证了名称会影响能力判断 |

三组压缩前后均有 OK 回复，没有客户端错误通知。测试目录配置已恢复，合成认证文件已移除。新的远程压缩请求没有触发额外 state 采集；正式前后生成按其各自流程处理。

**这证明当前修复与该客户端的压缩协议兼容，不证明真实 OpenAI 或任意中转必然接受同样请求。** 实际上后续真实 V2 成功、直接 V1 返回 404，正说明模拟上游成功不能替代真实验收；Team、注入与长会话仍要分别测。

## 本轮：OAuth 请求压缩兼容问题

真实 OAuth 联调的第一轮发现：恢复官方 provider 名称后，Codex 会发送 `Content-Encoding: zstd` 请求；旧代理只收未压缩 JSON，直接返回本地 415。该轮在进入模型链路前停止，实际模型尝试 **0 次**，不能把它写成“上游故障”。

随后加入 identity / gzip / zstd 解码，压缩输入和解压后正文均限制 16 MiB，zstd 窗口也设上限。解码之后仍校验 JSON、模型和 V2 压缩操作，移除旧编码声明再转发。

`encoding_test.go` 已覆盖并通过：生成解码转发、V2 识别、损坏/截断编码、未知及叠加编码、压缩与解压超限、zstd 超大窗口和模型限制。修复后的真实 OAuth 结果已列在上面的 11 次账本中：zstd 不再把这些请求拦在本地 415；Astra 后续两次短回复成功，但 292 和 V1 真实桥接仍未完成。请求编码修好不等于所有业务验收通过。

## 首版历史验收：当时确认到哪一步

**默认 10 块规则下，真实采集 → 注入 → Astra 回复已经跑通。** 不是把基线改成 11 块来通过测试。

2026-09-18，在 macOS / Apple Silicon、Codex CLI `0.154.0` 上，从真实 AnyTLS 出口采集到 292 字符、10 块 state。随后两次独立 CLI 请求都注入了同一份 state、使用同一出口，并收到 `OK` 回复，退出码均为 0。第二次没有重新采集。

这证明该次采集、代理出站、配置接管、注入和回复链路成立。**没有做回答质量对照实验，也没有证明可以消除账号限额。** 首版因此按公开测试版发布，不把一次链路验收写成长期效果保证。

## 真实测试怎么做的

```text
真实 Codex CLI → Go 服务 → 内嵌 AnyTLS 出站 → 真实 Astra 上游
```

- 使用独立的 Codex 目录和测试工作目录，由服务备份并配置 provider。
- CLI 运行在限制写入目录和网络出口的 macOS 沙盒里，不使用日常项目规则或会话。
- 对正式服务构建增加私有计数包装：最多放行 8 次模型请求尝试；只记状态、封装形状与指纹是否一致，不记录认证值和正文。这层包装不修改 state 或模型请求，未进入发布二进制。
- 本轮最终用了 7 次尝试，遇到合格候选就停止前台采集。两个生成请求都只发送一次。
- 测试用探测超时为 25 秒、冷却为 3600 秒，目的是限制联调次数；筛选基线仍是默认 10 块。发布默认超时与冷却分别为 20 秒、180 秒。

| 尝试 | 用途 | 结果 |
|---|---|---|
| 1–3 | 候选出口采集 | 连接失败，没有接纳候选 |
| 4 | 下一个候选出口采集 | HTTP 200、312 字符 / 11 块，但没有完整结束，不接纳 |
| 5 | 订阅标记为英国的 AnyTLS 出口 | HTTP 200、292 字符 / 10 块、SSE 完成，发布为当前值 |
| 6 | 第一条 CLI 正式请求 | 注入指纹与第 5 次一致；HTTP 200、SSE 完成、CLI 输出 `OK` |
| 7 | 第二条 CLI 正式请求 | 复用同一 state 和出口；HTTP 200、SSE 完成、CLI 输出 `OK` |

节点地区来自订阅标记，未把名称当作已验证的出口地理位置。测试只使用预先筛选的非港澳台标记节点，没有关闭 TLS 校验。

两次正式响应都没有重新附带 state 头。服务保留原值，不把缺失响应头当成新状态，也不自动清空它。

退出后，原 Codex 配置、登录文件和原代理配置的哈希均未改变。测试目录里的配置已恢复，临时复制的凭据已删除；没有刷新登录、切换系统代理或使用重置卡。真实订阅、节点和测试记录原件没有进入仓库。

## 前面遇到的问题与修复

第一次只测直连，返回的是 312；后来用另一组真实出口，也没有采到默认基线。将独立测试配置临时改为 11 块时，曾验证过注入协议能通，但这不算默认规则验收。上面的 292 实测才是后续结果。

排查发现并修复了这些实现问题：

- **AnyTLS 漏支持。** 现在 YAML、URI 导入均接入内嵌核心，有解析/构造测试和上述真实联调。
- **下载客户端标识影响订阅内容。** 可以逐条订阅设置 `user_agent`；空 `proxies` 会给出明确错误，不再混成编码错误。
- **需要明确选节点。** 增加协议白名单和名称/地址关键词排除，先过滤、再创建连接；不必为了混合订阅关闭 TLS 校验。
- **首条请求等备用采集。** 现在拿到第一份合格 state 就转发，备用采集留给后台并受冷却限制。
- **正式请求的账号错误没有统一暂停。** 现在探测与正式请求共享 401/403/429 处理，暂停期间不会因为已有 active 就继续发请求。
- **探测请求与源网关的 Codex 封装有差异。** 补齐 Responses 附加字段，不再强制 `low` 思考参数。单独对照这一改动时仍出现过 312，不能声称仅靠这项改动就能采到 292。

## 本地自动测试

使用 Go 1.27.1，在临时目录和本地模拟上游上执行，不使用真实账号：

| 检查 | 覆盖内容 |
|---|---|
| `go test -race -count=1 ./...` | 全包测试和竞态检查 |
| `go vet ./...` | 静态检查 |
| state fuzz | 10 秒约 160 万次输入解析，没有发现崩溃 |
| 配置事务 | 注释保留、接管、精确恢复、并发修改拒绝、符号链接保护 |
| 进程生命周期 | 真子进程启动、单实例锁、强制结束后恢复 |
| 采集与注入 | 默认基线拒绝 11 块、选择合格出口、state 与出口绑定、首条请求不等备用 |
| 正式请求 | 正文/会话链保留、SSE flush、取消传播、不自动重放 |
| 失败处理 | 探测和正式请求的 401/403/429、冷却、Retry-After、不继续换出口请求 |
| 订阅 | YAML/URI/Base64、AnyTLS、空节点配置、下载标识、协议/关键词筛选、危险字段拒绝 |
| HTTP / SOCKS5 | 本地 CONNECT、认证握手、域名传递与隧道数据 |
| 隐私 | 凭据、正文、节点名称、完整 state 不进入常规日志 |

其他加密协议的覆盖深度不相同。SS、VMess、VLESS、Trojan、Hysteria2 有解析和构造测试；本轮没有验证它们的所有真实服务商节点。SSR、Hysteria、TUIC 的实现来自内嵌核心。

依赖经 `govulncheck` 检查，初次修复后未发现调用路径可达的已知漏洞；仍提示 3 项模块层面的漏洞，未发现本程序调用路径。这个结果有时效，不等于没有安全风险。

## Windows 与发布验收

GitHub Actions 在 Windows、macOS、Linux runner 上执行 `go vet`、全包竞态测试和构建。Release 必须等三个系统的验证通过，再生成 Windows x64/ARM64、macOS Apple Silicon/Intel 四种包，以及含依赖的源码包和 SHA256 校验文件。

[查看对应提交和标签的运行结果](https://github.com/gylive/ccodex-sleep-state/actions)。**Windows runner 测试不是 Windows 桌面版 Codex 的完整实际使用测试。** Windows ARM64、Intel Mac 也尚未各自在桌面环境联调。

还需要继续观察：长会话上下文、不同客户端版本、state 过期与上游规则变化，以及实际任务中的质量差异。不能仅凭 `usable: true` 或 292 长度给回答质量打分。

## 遇到 `state_unavailable`

看日志中的 `probe_finished`，不要连续重发：

- `result: "shape_mismatch"`：请求完成了，但 `state_blocks` 不等于 `expected_blocks`。这是没采到符合规则的值，不等于节点断网。
- `result: "state_time_rejected"`：封装可读，但时间不符合本地规则。
- `result: "request_failed"`：连接、封装解析或 SSE 完成检查失败，结合状态码排查。

`status` 的 `phase` 会区分等待 state、已就绪、认证暂停和限流等待。历史版本没有合格值时保持严格模式。本轮转发内核空策略仍严格，但首次 `setup` 无明确策略时采用普通转发兜底，并保留用户已明确的 `strict`；它不注入不合格 state，也不把 312 当成 292 使用。

## v0.2.0-alpha.1：管理面板与配置兼容验收

2026-09-18，在独立数据目录、独立 Codex Home 下完成：

- 全部 Go 包的 `go test -race -count=1 ./...` 通过；`go vet ./...` 通过。
- 面板的固定 Host、同源校验、管理口令、静态资源 CSP；无口令与跨站操作被拒绝。
- 注入开关持久化、配置备份、外部编辑冲突、坏订阅不覆盖旧出口、本地文件只读、固定路由。
- 后台探测临界时收到 429，切换路由仍被拒绝；不会靠重建引擎丢掉限流状态。
- Codex provider/profile fixture 覆盖官方登录、中转 env_key、CCS API Key + requires_openai_auth、官方 API Key、外部配置或认证类型切换。
- 真浏览器完成登录、注入开关、代理配置解析测试、路由列表；390px 窄屏无横向溢出。

本轮授权最多 8 次真实 Astra 请求，实际使用 **4 次**：

1. 短请求采集：HTTP 200，SSE 完整结束，获得默认 10 块 / 292 长度 state。
2. 两次独立 Codex CLI 请求：都带该 state、使用同一出口，HTTP 200、完整结束、回复 OK、CLI 退出码 0。
3. 通过管理 API 关闭注入，再发一次 Codex CLI 请求：没有注入 state，HTTP 200、完整结束、回复 OK、CLI 退出码 0。

真实验收前后，原 Codex 配置、登录文件和代理配置哈希一致。测试目录的临时登录副本已删除，隔离配置已恢复；没有刷新登录、切换系统代理或使用重置卡。

**结论边界：**这证明本轮所用链路可采集、注入、关闭注入后正常转发，不证明降智已消失，不保证每个订阅都能取得 292。CCS / 中转兼容不等于对所有第三方中转站、所有 Windows 桌面客户端做过真实验收。

另用实际 Codex CLI 连接本地模拟中转，验收两种配置：`env_key` 与 CCS 常见的 `requires_openai_auth=true` + `auth.json` API Key。两组均自动接管原 `/v1` provider，正确传递 API Key，不带 ChatGPT 账号头或 turn-state，收到完整 SSE、回复 OK、退出码 0；停止后原配置恢复。此测试未使用真实登录、未访问第三方中转、未消耗模型额度。

## 2026-09-19：数组表启动修复与采集时间 WebUI

- 先在旧 `patch.go` 上运行 PR #2 的 `TestPatchConfigWithArrayTables`，复现 `reflect.Value.SetLen using unaddressable value` panic；纳入原 PR 后 `go test ./internal/codexconfig -count=1` 通过。
- `go vet ./...`、`go test -race -count=1 ./...` 全包通过。新增时间设置保存/回读/备份、不合法输入、缺鉴权、外部配置改动、固定出口丢失保护；既有 429 回归场景追加时间设置，确认不会清除限流状态。
- 浏览器在独立临时数据目录、本机 17849 端口、`serve --no-config` 且关闭注入下验收：低频预设填入 600 秒/2 次；确认保存后刷新页面仍保留；提前刷新设成等于 TTL 时表单拒绝；未保存的 750 秒输入在状态刷新后仍保留，撤销恢复已保存值。
- `node --check internal/service/web/app.js` 与 `bash -n scripts/build-release.sh` 通过。
- 本轮不使用真实账号请求、不改操作者的 Codex 配置、不消耗模型额度。这里的通过不代表真实上游质量改善或全部桌面平台联调通过。

### 可复现的本地打包

先确保源码已提交，运行 `VERSION=版本号 bash scripts/build-release.sh`。脚本拒绝覆盖已有输出目录，也拒绝未提交源码，生成 Windows amd64/arm64、macOS arm64/amd64 四个包、带 vendor 依赖的对应源码包及 SHA256SUMS。仅交叉构建不代表已在四种机器上实际运行；推送后还需查看 GitHub Actions 的原生系统测试结果。
