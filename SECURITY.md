# 隐私与安全

这个项目接触的是你的模型认证和网络出口。它不上传日志、不做遥测，但仍然是一个需要信任的本地程序。请只使用自己有权使用的账号和代理。

## 会接触哪些数据

| 数据 | 用途 | 是否落盘 |
|---|---|---|
| 订阅 URL、节点参数、代理密码 | 下载订阅、建立出站连接 | 程序不缓存订阅；面板保存或手工写入 JSON 时，配置及其备份会含明文 |
| Codex 认证头、账号头 | 按所选上游转发；仅官方 ChatGPT 注入模式发送短探测 | 转发使用请求头，只在内存中，不写回认证文件 |
| 所选 Codex 目录的 `auth.json` | 启动时只读判断 ChatGPT / API key / 含义不明确，只返回分类，不返回密钥 | 不复制、不修改、不刷新登录 |
| 完整 turn-state | active / ready 快照 | 只在内存中，重启重新采集 |
| 请求正文、模型返回正文 | 正常转发；模型校验需要暂存请求正文 | 不记录到日志；POST 正文上限 16 MiB |
| Codex `config.toml` 原文 | 自动接管前备份、恢复 | 同目录私有备份，可能含原本就存在的秘密 |
| 本地控制 token | 保护状态查询和 Web 管理动作 | 数据目录 `runtime.json` 和浏览器标签页会话存储；运行文件正常退出删除，旧口令重启失效 |
| 状态码、耗时、匿名出口 ID | 排错 | JSONL 轮换日志，约 2 MiB × 4 |

不要在 issue 中贴订阅 URL、完整配置、认证头、请求正文、state、内存转储或备份文件。通常只需要版本、系统、固定错误码，以及去掉个人信息的几行日志。

## 网络与配置边界

- 只允许监听字面值回环 IP，拒绝不匹配的 Host。模型转发路径拒绝浏览器 Origin / Fetch 请求；管理路径允许同源浏览器，但仍必须提供独立口令，拒绝跨站 Origin / Fetch。
- 管理端点使用独立随机 token；模型端点仍要求 Codex 的 Bearer 认证。页面使用限制性的 CSP，不加载 CDN 脚本。服务不是多人共享网关，不要用反向隧道或公网代理暴露它。
- 正常接管会解析 Codex 当前 provider / profile 并保留其上游和认证方式。API / 中转模式不采集官方 state、不使用官方账号头；可识别的官方 OAuth 凭据会被阻止误发。`--no-config` 时直接使用指定的服务上游，用户必须自行确认凭据归属。
- HTTPS 校验保持开启。订阅不能关闭证书验证、加载本地私钥、绑定系统网卡或改路由。
- 不跟随模型端点重定向。订阅下载只允许有限次数的 HTTPS 重定向，错误不会打印完整 URL。
- 不改变系统代理、不读代理软件的订阅库、不执行订阅中的规则、脚本或远程 provider。
- state 按认证头和账号头隔离。读取到新响应只记录形状信号，不能直接覆盖 active。
- 遇到 401、403、429 会暂停相应凭据的请求与采集；关闭注入、面板应用来源或固定路由不能清除暂停。状态保存在内存，不是不可绕过的账号策略系统。

## 配置恢复不是强行覆盖

接管前先生成独立备份，再保存带 SHA256 的恢复记录，最后替换配置文件。只有当前文件仍等于程序安装的版本时，才自动还原。发现其他程序或用户做过修改，就保留现场并报冲突。

意外退出后，再次启动也按哈希检查旧事务。运行中检测到 CCS 或其他工具改动接管配置时拒绝继续转发；请先停止服务，再切换 provider。

配置文件自身为符号链接时拒绝接管，避免把链接替换成普通文件。父目录的符号链接会解析到真实目录。两个程序同时编辑同一配置不是受支持的使用方式；停止其他配置切换工具再操作。

面板修改本工具配置前也创建独立备份；里面可能包含订阅密码。备份不会自动清理，因为它是恢复依据。确认不再需要后，由你自己处理。

## 威胁模型和局限

本地订阅文件只读，但读取权限仍是当前用户的权限。管理口令持有者能选择文件和网络来源，因此不要把口令交给不可信的人，也不要暴露面板。

服务防止的是常见误暴露、跨会话混用和误覆盖，不是同一用户权限下的恶意程序。Unix 的 0600/0700 不等于加密；Windows 的权限依赖用户目录 ACL。管理员、调试器、崩溃转储和备份软件仍可能接触内存或本地文件。

认证引用空闲 30 分钟后被移出会话表；Go 垃圾回收不保证及时覆盖原内存。需要强隔离的机器，请使用独立系统用户或虚拟机。

Fernet 外形解析**不会验证 HMAC，也不会解密**。它不能证明 token 的真实性、权限、模型智力或服务端 TTL。只接受当前凭据通过选定 HTTPS 上游拿到的候选，并不让这个经验方案变成官方协议。

项目不修改上游安全策略，不保证模型可用性。请遵守上游服务和代理供应商的使用条款。

## 报告问题

非敏感 bug 可以提交 GitHub issue。怀疑存在认证泄漏时，先不要公开重现材料；通过 GitHub 提供的私密安全报告渠道联系维护者。若仓库尚未启用该渠道，先发一个不含细节的联系请求，不要把秘密作为附件。
